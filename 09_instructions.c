#include <stdio.h>

int main(){
    int a = 4; //type declaration instruction.
    int b = 4, c, d; //type declaration instruction.
    printf("the value of a is %d\n", d); // Here, the output is the numeric value of memory locant at time of execution.
    printf("the value of a is %d\n", a);
    float x = 1.1;
    float y = x + 8.9;
    printf("the value of y is %f\n", y);
    return 0;
}
