#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("the area of circle is %f\n", pi * radius * radius);
    int height = 3;
    printf("the volume of cylinder with same radius is %f", pi * radius * radius * height);
    return 0;
}
