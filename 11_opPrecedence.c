#include <stdio.h>
// 1st * / %
// 2nd + -
// 3rd =
// in absense of parenthesis tie is brocken by associativity from left to right.
int main()
{
    int x = 4;
    int y = 9;
    printf("the value of 3x-4y is: %d", 3*x-4*y);
    return 0;
}